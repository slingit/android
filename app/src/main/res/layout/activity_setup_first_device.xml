<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="me.boopit.boopapp.SetupFirstDevice">

    <android.support.v7.widget.Toolbar
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/first_device_background"
        android:minHeight="128dip"
        android:layout_centerVertical="false"
        android:gravity="bottom|left"
        android:paddingBottom="12dip"
        android:titleTextAppearance="@style/Theme.BoopSetUpTheme.Title"
        />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/setup_relative_padding"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:id="@+id/relativeLayoutContent">
        <TextView
            android:id="@+id/setup_text"
            android:text="@string/setup_first_device"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:textAppearance="@style/TextAppearance.AppCompat.Medium"
            android:layout_marginBottom="0dip"/>
        <ImageView
            android:id="@+id/QRImageView"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:src="@drawable/sample_qr"
            android:layout_below="@id/setup_text"
            android:padding="18dip"
            android:paddingTop="0dip"
            android:layout_marginBottom="12dip"
            android:scaleType="fitCenter"
            android:adjustViewBounds="true"
        />
        <!-- what we display when the user doesn't have a camera -->
        <Button
            android:text="@string/setup_no_camera"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:textAppearance="@style/TextAppearance.AppCompat.Large"
            android:textColor="@color/blue"
            android:textStyle="italic"
            android:gravity="bottom|center"
            android:layout_alignParentBottom="true"
            android:background="@color/sweetSweetTransparency"
            android:onClick="noCamera"/>
    </RelativeLayout>

</RelativeLayout>
